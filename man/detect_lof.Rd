% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_lof.R
\name{detect_lof}
\alias{detect_lof}
\title{Multidimensional anomaly detection}
\usage{
detect_lof(df, k = 20, n_threads = parallel::detectCores())
}
\arguments{
\item{df}{A data frame or a numeric matrix.}

\item{k}{The number of nearest neighbors to consider, default is 20.}

\item{n_threads}{Number of threads to use. Defaults to all available cores.}
}
\value{
A numeric vector with LOF scores. Higher scores indicate greater anomaly potential.
}
\description{
The function calculates the Local Outlier Factor score for each row 
in the dataset, using parallel C++ processing and a KD-Tree.
Values close to 1 indicate "normal" observations. Values significantly greater than 1 
(e.g., > 1.5 or > 2.0) suggest distinct density-based anomalies.
}
\examples{
cluster <- data.frame(X = rnorm(100, 0, 0.5), Y = rnorm(100, 0, 0.5))
anomaly <- data.frame(X = 10, Y = 10)
dataset <- rbind(cluster, anomaly)
lof_scores <- detect_lof(dataset, k = 10, n_threads = 2)
print(lof_scores[101])
}
